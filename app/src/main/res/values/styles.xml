<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/netflix_rasin_black</item>
        <item name="colorAccent">@color/starGold</item>
        <item name="android:windowBackground">@color/netflix_rasin_black</item>
        <item name="android:textColor">@color/netflix_white</item>
    </style>

</resources>
